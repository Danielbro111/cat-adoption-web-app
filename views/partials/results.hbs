  <section id="searchResults">
    {{#if foundCatBreeds}}
      <h2>
          {{ catBreedTitle}}
      </h2>
    {{/if}}

    {{#each foundCatBreeds}}
      <section class="ui left aligned middle aligned segment">

        <h3 class="ui header">
            {{title}}
        </h3>

        <p>
          <i class="music icon"></i>
            {{#if songs}}
              Total songs:
              {{songs.length}}

              {{else}}
              Empty playlist!
            {{/if}}
        </p>
        
      </section>
    {{/each}}

  </section>
