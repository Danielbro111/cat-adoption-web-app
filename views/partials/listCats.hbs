{{#if  singleCat.catBreeds}}

  <table class="ui fixed striped table">
    <thead>
      <tr>
        <th>Song</th>
        <th>Artist</th>
      </tr>
    </thead>
    <tbody>
      {{#each singleCat.catBreeds}}
        <tr>
          <td>
            {{title}}
          </td>
          <td>
            {{artist}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  {{else}}
    <h3 class="ui message">
      Empty playlist!
    </h3>

{{/if}}
